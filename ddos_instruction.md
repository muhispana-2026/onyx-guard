We'll implement a load testing tool or instruction later. Right now we need to restore the C++ template string in App.tsx that got corrupted by the C# replace.
We need to copy the `ValidateClientState` C# block in App.tsx to see what happened.
